services:
  - type: web
    name: recipe-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m eventlet api.py
    envVars:
      - key: SQLALCHEMY_DATABASE_URI
        fromDotEnv: true
      - key: FLASK_ENV
        value: production
